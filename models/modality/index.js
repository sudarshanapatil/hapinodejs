const add = require('./add')
const destroy = require('./delete')
const edit = require('./edit')
const getById = require('./getById')
const list = require('./list')

module.exports = {
  add, destroy, edit, list, getById
}